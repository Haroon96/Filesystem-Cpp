#include <string>
using namespace std;

/* converts an integer to string with specified length */
string itos(int, int);
/* truncates a string after a delimiter */
string truncate(string&, char);
/* splits a string into sub strings*/
void strTok(string, string[]);
/* converts uppercase to lowercase */
string lowercase(string);